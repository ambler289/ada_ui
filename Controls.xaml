<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style x:Key="Ada.Card" TargetType="Border">
    <Setter Property="CornerRadius" Value="{StaticResource Ada.Radius}"/>
    <Setter Property="Background" Value="{StaticResource Ada.Brush.Surface}"/>
    <Setter Property="BorderBrush" Value="{StaticResource Ada.Brush.Border}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="{StaticResource Ada.Padding.Card}"/>
  </Style>

  <Style x:Key="Ada.Button" TargetType="Button">
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="Background" Value="{StaticResource Ada.Brush.Blue}"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="18,8"/>
    <Setter Property="MinWidth" Value="110"/>
    <Setter Property="MinHeight" Value="36"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="bd"
                  Background="{TemplateBinding Background}"
                  CornerRadius="{StaticResource Ada.Radius}">
            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="Opacity" Value="0.95"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="bd" Property="Opacity" Value="0.85"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="bd" Property="Opacity" Value="0.45"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="Ada.Button.Secondary" TargetType="Button" BasedOn="{StaticResource Ada.Button}">
    <Setter Property="Background" Value="{StaticResource Ada.Brush.Pink}"/>
  </Style>

  <Style x:Key="Ada.TextBox" TargetType="TextBox">
    <Setter Property="Foreground" Value="{StaticResource Ada.Brush.Text}"/>
    <Setter Property="Background" Value="#171B22"/>
    <Setter Property="BorderBrush" Value="{StaticResource Ada.Brush.Border}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="8,6"/>
    <Setter Property="FontFamily" Value="{StaticResource Ada.Font}"/>
    <Setter Property="FontSize"   Value="14"/>
  </Style>

  <Style x:Key="Ada.ListBox" TargetType="ListBox">
    <Setter Property="Foreground" Value="{StaticResource Ada.Brush.Text}"/>
    <Setter Property="Background" Value="#171B22"/>
    <Setter Property="BorderBrush" Value="{StaticResource Ada.Brush.Border}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="FontFamily" Value="{StaticResource Ada.Font}"/>
    <Setter Property="FontSize"   Value="14"/>
  </Style>

  <Style x:Key="Ada.ListBoxItem" TargetType="ListBoxItem">
    <Setter Property="Padding" Value="6,4"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListBoxItem">
          <Border x:Name="bd" Background="{TemplateBinding Background}">
            <ContentPresenter Margin="2"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="bd" Property="Background" Value="#26304A"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="bd" Property="Background" Value="#202538"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="Ada.HeaderBar" TargetType="Grid">
    <Setter Property="Height" Value="48"/>
    <Setter Property="Background" Value="{StaticResource Ada.Brush.HeaderGrad}"/>
    <Setter Property="Margin" Value="0,0,0,12"/>
  </Style>
</ResourceDictionary>
